- name: Demo on filters
  hosts: local
  connection: local
  vars:
    topics: linux, shell, ansible
  tasks:
  - name: Convert string to list
    ansible.builtin.debug:
      msg: "{{ topics | split(',') }}"
      