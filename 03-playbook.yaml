- name: install and start nginx
  hosts: web
  become: yes
  tasks:
  - name: install ngnix
    ansible.builtin.package:
      name: nginx
      state: present

  - name: start ngnix 
    ansible.builtin.service:
      name: ngnix
      state: started
      enabled: yes