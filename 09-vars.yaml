- name: "vars from command line"
  hosts: local
  connection: local
  tasks:
  - name: vars from command line
    ansible.builtin.debug:
      msg: "My name is {{ NAME }}"

# anisble-playbook -i inventory.ini -e "NAME=Niteesh Gandham" 09-vars.yaml